{
  "name": "conmed-recon",
  "private": true,
  "version": "0.1.0",
  "description": "Root scripts for hosting the admin panel (frontend/) and voice backend (voice/) on one machine.",
  "scripts": {
    "install:all": "npm --prefix frontend ci && npm --prefix voice ci",
    "frontend": "npm --prefix frontend run build && npm --prefix frontend run preview -- --host 0.0.0.0 --port ${FRONTEND_PORT:-5173}",
    "frontend:dev": "npm --prefix frontend run dev",
    "api": "node --no-warnings api/server.mjs",
    "seedDb": "node --no-warnings voice/brain/db/seed.js && node --no-warnings scripts/seed-voice-db.mjs",
    "voice": "PORT=${VOICE_PORT:-8787} npm --prefix voice start"
  }
}
