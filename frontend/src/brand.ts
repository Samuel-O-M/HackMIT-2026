/**
 * The product name and the site this install serves. One place, because it
 * appears on the sign-in card, the rail, and the browser tab.
 */
export const PRODUCT_NAME = 'Participant Medication Records';
export const SITE_LABEL = 'Site 042';
