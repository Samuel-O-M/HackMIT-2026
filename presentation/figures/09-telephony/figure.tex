% Slide 09 — how the call reaches a phone.
%
% Worth a slide because "a voice agent calls the participant" invites the
% question "calls them how?", and the deck never answered it.
%
% The demo-versus-real distinction used to be two large columns here, which
% put the weight on what we did not do. It is a footnote: the interesting
% claim is that a call gets placed at all, and that the thing refuses to dial
% a number that could belong to a person.
\documentclass[border=0pt]{standalone}
\input{shared/theme}
\begin{document}
\hyphenpenalty=10000\exhyphenpenalty=10000
\begin{tikzpicture}[x=1mm,y=1mm]
\path[use as bounding box] (0,0) rectangle (160,90);

\node[headline] at (16,82) {It really does place a call};
\node[subline]  at (16,75.5) {The coordinator never dials, and the participant never has to open anything.};

% ---- the flow -----------------------------------------------------------
\foreach \x/\step/\detail in {%
  14/{Press Call}/{on the participant's row in the visit list},
  50/{The phone rings}/{wherever that handset is},
  86/{They answer}/{and the agent speaks first, as a caller should},
  122/{The call runs}/{identity, the log, side effects, protocol rules}}{%
  \fill[surf, rounded corners=2pt] (\x,48) rectangle (\x+28,68);
  \draw[line, line width=0.4pt, rounded corners=2pt] (\x,48) rectangle (\x+28,68);
  \node[anchor=north, font=\fontsize{7.4}{9}\selectfont\sffamily\bfseries\color{ink}] at (\x+14,65) {\step};
  \node[anchor=north, font=\fontsize{5.6}{6.9}\selectfont\sffamily\color{inkii}, align=center, text width=24mm] at (\x+14,59) {\detail};
}
\foreach \x in {43, 79, 115}{ \draw[flow] (\x,58) -- (\x+6,58); }

% ---- the guard ----------------------------------------------------------
\fill[surf, rounded corners=2.5pt] (14,16) rectangle (146,40);
\draw[brand, line width=0.7pt, rounded corners=2.5pt] (14,16) rectangle (146,40);

\node[anchor=north west, font=\fontsize{5.6}{6.8}\selectfont\sffamily\bfseries\color{brand}] at (19,37) {THE PART THAT MATTERS};
\node[anchor=north west, font=\fontsize{9}{11}\selectfont\sffamily\bfseries\color{ink}] at (19,32)
  {It will not dial a number that could belong to a real person.};
\node[anchor=north west, font=\fontsize{6.2}{7.6}\selectfont\sffamily\color{inkii}, align=left, text width=120mm] at (19,24)
  {Only 555-0100 to 555-0199 is reserved as permanently unassignable. Every other number is refused unless someone deliberately overrides it.};

% ---- footnote -----------------------------------------------------------
\node[anchor=west, font=\fontsize{5.8}{7}\selectfont\sffamily\color{inkiv}, align=left, text width=132mm] at (14,8)
  {The demo rings a handset over the site's own network; the carrier path for an ordinary telephone is written and switched on with one setting. Everything after ``hello'' is the same either way.};

\end{tikzpicture}
\end{document}
