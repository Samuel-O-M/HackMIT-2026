% Slide 05c — the protocol parser.
%
% The deck never said that a trial is set up by dropping in its protocol PDF,
% which is the thing that makes this work for any study rather than three
% hardcoded ones.
%
% Drawn rather than described: a page with one section lit, an arrow, and the
% rules that came out of it. The badges carry the distinction that took the
% most work — a rule that bites during treatment against one the participant
% already satisfied at screening — without a sentence explaining it.
\documentclass[border=0pt]{standalone}
\input{shared/theme}
\begin{document}
\hyphenpenalty=10000\exhyphenpenalty=10000
\begin{tikzpicture}[x=1mm,y=1mm]
\path[use as bounding box] (0,0) rectangle (160,90);

\node[headline] at (16,82) {Drop in the protocol. It reads it.};

% ---- the document -------------------------------------------------------
\fill[surf, rounded corners=1.5pt] (16,16) rectangle (56,70);
\draw[line, line width=0.5pt, rounded corners=1.5pt] (16,16) rectangle (56,70);

% body text, as texture
\foreach \y in {66,63.5,61,58.5,56,53.5,51,48.5}{%
  \fill[surfiii, rounded corners=0.3pt] (20,\y) rectangle (52,\y+1.1);}
\fill[surfiii, rounded corners=0.3pt] (20,46) rectangle (42,47.1);

% the section that matters, lit
\fill[brand!12!white, rounded corners=1pt] (18,28) rectangle (54,43);
\draw[brand, line width=0.5pt, rounded corners=1pt] (18,28) rectangle (54,43);
\node[anchor=north west, font=\fontsize{6.4}{7.7}\selectfont\sffamily\bfseries\color{brand}] at (20.5,41) {\S5.7.2};
\foreach \y in {33,30.6}{ \fill[brand!35!white, rounded corners=0.3pt] (20.5,\y) rectangle (51,\y+1.1);}
\fill[brand!35!white, rounded corners=0.3pt] (20.5,35.4) rectangle (46,36.5);

\foreach \y in {24,21.5}{ \fill[surfiii, rounded corners=0.3pt] (20,\y) rectangle (52,\y+1.1);}

\node[anchor=north, font=\fontsize{6.4}{7.7}\selectfont\sffamily\color{inkii}] at (36,13) {the published protocol};

% ---- the arrow ----------------------------------------------------------
\draw[flow, line width=1pt] (60,43) -- (70,43);

% ---- what comes out -----------------------------------------------------
% Written out rather than looped: a \foreach variable does not expand inside
% a node's option list, so the badge colour has to be literal.
\newcommand{\rulecard}[4]{% #1 base y  #2 rule  #3 badge text  #4 badge colour
  \fill[surf, rounded corners=2pt] (74,#1) rectangle (146,#1+12);
  \draw[line, line width=0.4pt, rounded corners=2pt] (74,#1) rectangle (146,#1+12);
  \node[anchor=west, font=\fontsize{7}{8.5}\selectfont\sffamily\bfseries\color{ink}] at (78,#1+7.8) {#2};
  \node[anchor=west, font=\fontsize{5.6}{6.8}\selectfont\sffamily\bfseries, inner xsep=1.4mm, inner ysep=0.8mm,
        rounded corners=1.2pt, fill=#4!12!white, text=#4] at (78,#1+3.6) {#3};
}

\rulecard{58}{Chronic immunosuppressants}{during treatment}{brand}
\rulecard{43}{Systemic corticosteroids}{above 10 mg a day}{brand}
\rulecard{28}{Live attenuated vaccines}{before \textbf{and} during}{caution}

\node[anchor=north, font=\fontsize{6.4}{7.7}\selectfont\sffamily\color{inkii}] at (110,13) {rules the agent can check against};

\end{tikzpicture}
\end{document}
